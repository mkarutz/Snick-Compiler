proc main ()
	int m;
	int n;
	read n;
	while n > 1 do
		m := n;
		while m > 0 do
			if m > 0 then
				n := (2 * n) / 2;
				m := m - 1;

				if m = 0 then
					p(n);
				fi
			else
				m := n - m;
				m := m - 1;
			fi
		od
	od
end

proc p (ref int i)
	i := i - 2;
end

proc q (val float x, ref int k)
	int n;
	float y;
	bool a[1..8,3..4];
	a[8] := true;
	k := 42;
end
